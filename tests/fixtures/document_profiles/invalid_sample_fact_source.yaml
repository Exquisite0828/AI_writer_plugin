profile_id: test.invalid_sample_fact_source
profile_version: 0.1.0
task_type: custom_technical_note
display_name: Invalid Custom Technical Note
description: Invalid profile used only for validation tests.
default_sections:
  - Background
required_sections:
  - background
optional_sections: []
critical_claims:
  - deployment risk
requires_human_confirmation:
  - deployment risk
forbidden_final_claims:
  - no deployment risk exists
confirmation_marker: NEEDS_USER_CONFIRMATION
fact_source_roles:
  - source
  - sample
non_fact_source_roles:
  - template
  - checklist
  - reference
reference_policy: Reference materials must not prove project-specific facts.
sample_policy: Sample documents must not supply project facts.
default_final_status: ready_for_human_review
allowed_final_statuses:
  - ready_for_human_review
review_focus:
  - sample misuse
verification_focus:
  - sample not fact source
candidate_learning_policy: Generate candidate updates only; keep proposed/inactive unless explicitly approved.
terminology: {}
output_labels: {}
