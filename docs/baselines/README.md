# Baselines

This directory stores structural baseline records used for regression protection.

Baseline documents summarize verified behavior, required artifacts, and non-regression contracts. They are not generated run dumps and must not contain full draft, review, final report, trace, or learning artifact contents.

Generated runtime artifacts stay under ignored `runs/<run_id>/` directories and must not be committed.
